#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - entry point of the program
 * description: variables, if, else and while
 * Return: Always 0 (Success)
 */

int main(void)
{
	int hx, lc;

	for (hx = 48; hx < 58; hx++)
		putchar(hx);
	for (lc = 97; lc < 123; lc++)
		putchar(lc);
	putchar(10);
	return (0);
}
