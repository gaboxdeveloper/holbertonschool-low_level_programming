#include "main.h"

/**
 * _abs - function
 *
 * @n: asdasd
 *
 * Return: 0
 */

int _abs(int n)
{
	unsigned int a;

	if (n == 0)
		_putchar(n);
	else if (n < 0)
	{
		a = n;
		_putchar(a);
	}
	else
		_putchar(n);
	return (0);
}
