#include "main.h"

/**
 * _abs - function
 *
 * @n: asdasd
 *
 * Return: 0
 */

int _abs(int n)
{
	unsigned int a;

	if (n == 0)
		return(n);
	else if (n < 0)
	{
		a = n;
		return(a);
	}
	else
		return(n);
}
